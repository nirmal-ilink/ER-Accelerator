# ER-Accelerator Dependencies
# Note: Databricks Connect officially supports Python 3.10-3.12
# Python 3.13 may have compatibility issues

# Databricks Connect - Remote Spark execution on Databricks cluster
# This provides PySpark APIs, so separate pyspark is not needed
databricks-connect>=14.3.0

# Web Framework
streamlit>=1.30.0

# Data Processing
pandas>=2.0.0
pyyaml>=6.0

# Utilities
python-dotenv>=1.0.0

# Note: pyspark removed - databricks-connect provides Spark APIs
# If running locally without Databricks, install pyspark separately:
#   pip install pyspark
